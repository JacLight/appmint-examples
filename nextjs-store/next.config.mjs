/** @type {import('next').NextConfig} */
export default {
  // Product images come from wherever the catalog points. A demo should not
  // fail on an unknown host, so images are rendered with a plain <img>.
  reactStrictMode: true,
};
